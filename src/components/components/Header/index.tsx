import { Button } from '@/components/global/Button';
import * as S from './styles';

export const Header = () => {
  return (
    <S.HeaderContainer>
      teste
      <Button />
    </S.HeaderContainer>
  );
};
